next-version: 0.1.0
mode: ContinuousDelivery
branches:
  master:
    regex: ^main$
    tag: beta
    increment: none
  feature:
    mode: ContinuousDeployment
    regex: feature[/-]
    increment: none
ignore:
  sha: []
merge-message-formats: {}
